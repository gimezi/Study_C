// A + B
#include <stdio.h>

int main(void)
{

	int a, b, c;
	scanf("%d %d", &a, &b);

	c = a + b;

	printf("%d", c);

	return 0;
}

// A - B
#include <stdio.h>

int main(void)
{

	int a, b, c;
	scanf("%d %d", &a, &b);

	c = a - b;

	printf("%d", c);

	return 0;
}


// A * B
#include <stdio.h>

int main(void)
{

	int a, b, c;
	scanf("%d %d", &a, &b);

	c = a - b;

	printf("%d", c);

	return 0;
}

// A / B
#include <stdio.h>

int main()
{
	double a, b;
	scanf("%lf %lf", &a, &b);
	printf("%.10f", a / b);
	return 0;
}