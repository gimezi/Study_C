# include <stdio.h>
# include <string.h>

int main() {
  alpha[26];
  cha[100];

  scanf("%s",cha,100);

  memset(alpha, -1, sizeof(alpha) );

  printf(*alpha)
  for (int i='a'; i <= 'z'; i++) {
    for (int j =0; j <strlen(alpha); j++){

    }

  }
}